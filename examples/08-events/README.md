# Events

Shows keyboard and mouse event handling through the `KeyListener` interface, `KeyMap` bindings, and helpers like `OnKey`, `OnRune`, and `OnRunesStop`.

## Screenshot

![Events](../../docs/public/guides/08.png)

## Run

```bash
go run .
```

## Guide

For a detailed walkthrough, see the [Events guide](https://go-tui.dev/guide/events).
